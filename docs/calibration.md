# Ledger de calibração

Registro de probabilidade estimada versus resultado real por passo do plano de absorção do EasyStok
(plans/easystok-absorcao-total.md). Atualizar é parte do Definition of Done de cada passo.

P = M x C x R (maturidade do componente x cobertura de teste do caminho x precedente copiável).
Detalhe da fórmula na seção de medição do plano. Após cada passo, comparar o estimado com o real
calibra os pesos do próximo.

| Passo | P estimado | Fechou de primeira | Nota |
|---|---|---|---|
| 0.0 ADR-016 deploy k3s e KEDA no EasyStok | 0.90 | Sim | ADR de deploy: k3s single-node co-residente, Postgres compartilhado com database `hiram` e role de menor privilégio (sem RLS, sem superuser, sem BYPASSRLS), PgBouncer só para o Hiram (caminho do ERP intocado), orçamento de conexões versionado e `oom_score_adj` do Postgres. Topologia decidida com o usuário (k3s mais KEDA sobre compose co-localizado). Sem código, aceite do ADR. |
| 0.1 Dockerfiles Api e Dispatcher | 0.85 | Sim | Multi-stage non-root (.NET 10), build a partir da raiz, restore cacheado, publish Release (warnings as errors). Api serve em +:8080 via override de Kestrel por env; Dispatcher sem HTTP (runtime image, liveness por pgrep no k8s). `.dockerignore` enxuga o contexto e tira appsettings.Development.json da imagem. Job `docker-images` no CI valida o build das duas imagens. Local: `dotnet publish -c Release` dos dois OK; `docker build` valida no CI (sem Docker no dev). |
| 0.2 Secrets via env e .env.hiram.example | 0.90 | Sim | Sem bug: o host já lê toda a config de env (config padrão do ASP.NET; user-secrets é só dev). Teste de caracterização `Host_Boots_FromEnvOnly` sobe a Api em Production só com env (sem appsettings.Development, sem user-secrets) e serve /openapi/v1.json, travando o comportamento (verde de saída, Docker-free). `.env.hiram.example` documenta todas as variáveis dos dois hosts. Release sem warning. |
| 0.5 Health e readiness na Api | 0.75 | Sim | `/health/live` (sem checks, sempre 200) e `/health/ready` (checks custom de Postgres via CanConnectAsync e Redis via PingAsync, sem lib nova logo sem ADR) na Api; públicos por estarem fora de /v1. Vermelho (404) para verde (ready 503 com deps fora, live 200), Docker-free. Dispatcher sem HTTP: liveness por pgrep no manifesto k8s (0.9). Release sem warning. |
| 0.6 Graceful shutdown com draining | 0.80 | Sim | Base `RabbitConsumerWorker` extraída dos 3 consumers idênticos (Email/Push/Webhook viram subclasses finas), com contador in-flight e override de StopAsync: BasicCancel para parar novas entregas, espera o drain (helper `ConsumerDrain`, grace 30s) e só então fecha o canal. Seguro por fallback: timeout volta ao at-least-once (redelivery, sem perda). Helper testado Docker-free (vermelho→verde); caminho de consumo coberto pelos E2E no CI. Não adicionei teste de shutdown Testcontainers para não arriscar flakiness sem Docker local para verificar. Release sem warning, unit 112/112. |
| 0.7 Database hiram, role de menor privilégio, backup | 0.85 | Sim | SQL provisiona o database `hiram` (idempotente via \gexec) e o role hiram com NOSUPERUSER/NOBYPASSRLS/NOCREATEDB/NOCREATEROLE, fechando a classe do P0 de RLS do EasyStok. Backup lógico do banco mais tarball do volume do key ring (perda do key ring = segredos indecifráveis). Isolamento cruzado (REVOKE CONNECT na base do EasyStok) documentado como ação do operador, não executado cross-DB. Artefatos de ops, validação no ambiente real, sem teste unitário. |
| 0.8 PgBouncer dedicado e orçamento de conexões | 0.70 | Sim | PgBouncer em transaction pooling só para o Hiram (ERP intocado; incompatível com os advisory locks de sessão do outbox do EasyStok). pgbouncer.ini com pool de servidor fixo, userlist template sem segredo. connection-budget.md crava o invariante (pool_servidor + ERP + reserva < max_connections), exemplo trabalhado e os passos para confirmar contra a VM. Números finais e settings Npgsql validam no ambiente real. |
| 0.9 Manifests k3s | 0.65 | Sim | namespace, ConfigMap, Secret template, PVCs (key ring RWO compartilhado, RabbitMQ persistente), RabbitMQ StatefulSet, Redis efêmero, PgBouncer, Job de migração --migrate-only, Api (probes /health/*, requests/limits, volume do key ring com fsGroup, securityContext hardening) e Dispatcher (liveness por /proc, keyring, terminationGracePeriod para o drain). YAML validado com yq local e job de lint no CI (python). Smoke real em k3s é gate do operador/ambiente (sem cluster aqui). oom_score_adj do Postgres documentado como ação no compose do EasyStok, não k8s. |
| 0.10 KEDA ScaledObject | 0.70 | Sim | ScaledObject escala o Dispatcher por QueueLength das filas email/push/webhook (KEDA usa a mais demandada), min 1, maxReplicaCount amarrado ao orçamento de conexões (passo 0.8). TriggerAuthentication usa o amqp host do secret. YAML validado com yq local e lint no CI. Escala real e o maxReplicaCount final validam no cluster contra o orçamento. |
| 0.11 Observabilidade LGTM e SLOs | 0.85 | Sim | LGTM single-container (grafana/otel-lgtm) com OTLP 4317/4318 e Grafana 3000; ambos os hosts já exportam para ele via OTEL_EXPORTER_OTLP_ENDPOINT. slos.md crava SLIs/SLOs com PromQL aproximado, alertas por queima de error budget, orçamento de conexões e as 3 séries de paridade do gate de corte. Dashboards/alertas finais e o teste Otel_FromBothHosts validam no cluster real. YAML validado com yq e lint no CI. |
| 1.0 ADR-017/018/020 e contratos congelados | 0.80 | Sim | Três ADRs (ingestão+motor+idempotência de dois níveis+recuperação+matching de rotina; destinatário/contato/consentimento cross-channel; adiamento por dispatch_at). Contratos em Hiram.Contracts (SubmitEventRequest com emission_seq, EventRecipient, EventAccepted) e docs/contracts/v1-events.md com o mapa real dos 37 tipos (lido do enum TipoEventoNotificacao), mapa de canais por nome (ordinais divergem), labels OTel, regra de tipo desconhecido e canonicalização. Release compila; cliente do EasyStok por codegen do OpenAPI, não espelho manual. |
| 1.1 POST /v1/events transacional e idempotência de evento | 0.80 | Sim | Núcleo: NotificationEvent + IngestEventHandler, idempotência por event_id, vermelho→verde Docker-free (unit 114/114, +2). Infra: EventStore transacional (evento + outbox numa transação; unique violation vira DuplicateEventException), EventConfiguration, DbSet, migration AddEvents (tabela events, índice único (tenant_id,event_id), reversível e aditiva), endpoint POST /v1/events, DI, fila de eventos na topologia (mensagens esperam o consumer do 1.2, não viram não-roteáveis). EventStoreTests (Testcontainers) validam no CI. Release 0/0. |
| 1.2 Motor de rotinas (resolver + fiação) | 0.70 | Sim | Núcleo: `Routine` + `RoutineResolver` (decisão pura) com os 3 comportamentos nomeados vermelho→verde Docker-free (matching de N ativas, zero vira NoRoute, template não aprovado vira suppressed com motivo). Fiação: `RoutineCatalog` (EF, ativos tenant+global por evento), `TemplateApprovalLookup` (via store de template; aprovação real no 1.3), persistência de Routine (config com conversão CSV dos canais, migration AddRoutines validada na geração), DI e RoutineResolver registrado. Lookup testado Docker-free; RoutineCatalogTests no CI. Pendente: o consumer da fila de eventos que produz o fan-out (depende de render/canais, 1.6/1.8). Release 0/0, unit 117/117. |
| 1.3 Template estendido (aprovação/versão/checksum) | 0.70 | Sim | Template ganha Approved/Version/Checksum; editar conteúdo bumpa versão, recomputa checksum (SHA-256 de subject+body) e reseta aprovação (re-aprovação obrigatória). Endpoint POST /v1/templates/{id}/approve + store ApproveAsync. TemplateApprovalLookup passa a usar Approved real (não mais "existe = aprovado"); 3 testes Docker-free (ausente / existe-não-aprovado / aprovado). Migration AddTemplateApproval (colunas aditivas com defaults). A cópia de dados dos templates do EasyStok é script ops com dry-run no live. Release 0/0, unit 117/117. |
| 1.4 Consentimento + destinatário + dual-write | 0.65 | Sim | Núcleo: Consent (domínio), ConsentPolicy (opt-out filtra canal; default transacional/operacional permitido, marketing exige opt-in) e ConsentReconciler (dual-write last-write-wins por timestamp), vermelho→verde Docker-free. Infra: ConsentStore EF (upsert por tenant+user+channel+category), config, migration AddConsents, endpoint POST /v1/consent (alvo do dual-write do EasyStok), DI. Contato vai no payload do evento (ADR-018). Consentimento de SMS/WhatsApp/InApp chega com esses canais. ConsentStoreTests no CI. Release 0/0, unit 119/119. |
| 1.5 Bloqueio/kill-switch | 0.80 | Sim | Block (global/canal/tenant, expiração, remoção) + BlockGate (suprime se há bloqueio ativo casando o canal), vermelho→verde Docker-free. Infra: BlockStore EF (ativos por tenant+global; RemoveAsync tenant-scoped para isolamento), config, migration AddBlocks, endpoints POST/DELETE /v1/blocks, DI. BlockStoreTests (Testcontainers) no CI. Release 0/0, unit 121/121. |
| 1.6 Resolver de canais + fallback (núcleo) | 0.75 | Sim | ChannelResolver aplica bloqueio + consentimento na ordem de fallback da rotina, preservando a ordem; vermelho→verde Docker-free (ordem preservada; ambos os filtros aplicam). DI registrado. O consumer da fila de eventos que monta o fan-out converge com 1.7 (dispatch_at) e 1.8 (idempotência de mensagem) e entra ao fim. Release 0/0, unit 123/123. |
| 1.7 Adiamento (dispatch_at + jitter + janela) + limite diário | 0.70 | Sim | Núcleo: WindowScheduler (adia para a abertura da janela no fuso do tenant; jitter espalha; nunca suprime) e DailyLimitPolicy (limite>0 e count>=limite), 3 testes nomeados vermelho→verde Docker-free. Infra (ADR-020, fenceada): coluna dispatch_at no outbox, query do relay vira `dispatch_at IS NULL OR dispatch_at <= now()` (relógio do banco), migration AddOutboxDispatchAt, DI do scheduler (jitter até 5 min). Asserção de regressão DirectNotification_StillPublishesImmediately_WithDeferralQuery + deferred-held no OutboxRelayTests (CI). Release 0/0, unit 126/126. |
| 1.8 Idempotência de mensagem no fan-out + claim | 0.70 | Sim | Núcleo: MessageKey (hash determinístico de event_id+channel+recipient+template_version+dispatch_slot; transacional colapsa o slot) e MessageDispatchGuard (claim → envia; replay/redelivery não reenvia), vermelho→verde Docker-free. Infra: MessageClaim + MessageClaimStore EF (claim durável; unique violation → false com detach para reusar contexto), config, migration AddMessageClaims, DI. MessageClaimStoreTests no CI. O claim na mesma transação do DeliveryAttempt fecha no consumer (convergência). Release 0/0, unit 129/129. |
| 0.3 Data Protection key ring compartilhado | 0.80 | Sim | Fix localizado em `AddHiramDataProtection` (SetApplicationName mais PersistKeysToFileSystem) e wiring nos dois hosts via `DataProtection:KeysPath`. O teste cross-process exigiu um probe dedicado para ser um segundo processo do SO real, não in-proc. Vermelho reproduziu o bug exato (key not found in key ring), verde passou. Release sem warning, unit 112/112. Suíte Testcontainers não roda no dev local (Docker ausente), validação no CI. |
| 0.4 Migrations em produção via Job --migrate-only | 0.85 | Sim | `HiramSchema` (GenerateScript idempotente via IMigrator mais ApplyAsync); dispatch `--migrate-only [--dry-run]` no host; gate de Development trocado por flag `Hiram:MigrateOnStartup` (decisão tomada com o usuário) ligada em appsettings.Development.json, para não quebrar 9 testes que sobem o host. Dry-run Docker-free vermelho para verde local mais smoke do host real (exit 0, script idempotente sem DB). Ajuste em voo: top-level Main virou Task int, exigiu return explícito. Migrate_OnEmptyDb e Migrate_OnCurrentDb precisam de Docker, validam no CI. Release sem warning, unit 112/112. |
